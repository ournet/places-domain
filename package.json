{
  "name": "@ournet/places-domain",
  "version": "0.1.2",
  "description": "Ournet places domain",
  "main": "./lib/index.js",
  "types": "./types/index.d.ts",
  "scripts": {
    "pretest": "npm run clean && npm run tsc",
    "test": "ava --no-cache --verbose lib/**/*.test.js",
    "posttest": "npm run clean-test-code",
    "tsc": "tsc",
    "tscw": "tsc -w",
    "preversion": "npm run tsc",
    "postversion": "git push --follow-tags",
    "prepublishOnly": "npm run tsc && npm run clean-test-code",
    "clean": "rm -fr ./lib && rm -fr ./types",
    "clean-test-code": "rm -fr ./lib/**/*.test.js && rm -fr ./types/**/*.test.d.ts"
  },
  "engines": {
    "node": ">=6.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Ournet/places-domain.git"
  },
  "keywords": [
    "ournet",
    "places",
    "geonames",
    "domain"
  ],
  "author": "Dumitru Cantea",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/Ournet/places-domain/issues"
  },
  "homepage": "https://github.com/Ournet/places-domain",
  "dependencies": {
    "@ournet/domain": "^0.1.0",
    "joi": "^13.0.2"
  },
  "devDependencies": {
    "@types/joi": "^13.0.0",
    "@types/node": "^8.x.x",
    "ava": "^0.23.0",
    "typescript": "^2.6.1"
  }
}
